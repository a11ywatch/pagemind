syntax = "proto3";

// CDN that is backed by S3
service Cdn {
   rpc AddScript (ResourceParams) returns (Empty) {}
}

// the resource to store into the cdn
message ResourceParams {
   string scriptBuffer = 1;
   string domain = 2;
   string cdnSourceStripped = 3;
}

// empty message
message Empty {}